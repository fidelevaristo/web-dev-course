function getPopulation(): any {
  return 69_950_850;
}

// Population is a number
const population = getPopulation() as number;

// Double the population value
console.log(population * 2);

export {};
